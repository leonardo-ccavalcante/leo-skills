# Decision record: <short title>

<!-- One file per decision, saved as bp_<slug>/decisions/DR-<nnn>-<slug>.md.
     Decision records are how choices survive across sessions: Step 1 of every
     route reads this directory before writing anything. -->

<!-- Metadata block. The Type line carries exactly one word and nothing else:
     `decision` for a fresh choice, `correction` for a reversal or fix of an
     earlier one, `deferral` for a choice consciously postponed. `bp.sh
     retro-score` reads that line mechanically and counts the corrections into
     the session scorecard, so a Type line left holding the whole menu counts as
     whichever word the scorer matches first. Replace the placeholder with one
     word; never list the options on it; type a reversal as a correction rather
     than as a fresh decision. -->

- Date: <YYYY-MM-DD>
- Type: <type>
- Route / part: <FULL-PLAN part n, MODULE name, or EVALUATE>

## Decision

<what was decided, one paragraph, in the past tense>

## Alternatives considered

| Alternative | Why rejected |
|---|---|
| <option> | <reason, with its evidence tag where a number is involved> |

## Evidence

<!-- Tags reference the Inputs & Sources table or saved artifacts; a decision
     resting only on assumptions says so here. -->

- <input or artifact this decision rests on, with its tag>

## Revisit when

<!-- A concrete trigger, not a date habit: a metric crossing a threshold, a
     source publishing new data, an experiment finishing. -->

- <trigger that reopens this decision>
