# kb-verify memory

<!-- heuristics, not instructions. English, at most 60 lines, curated by humans.
     `kbv memory-append` (via /kb-learn) appends lines `L-nn <statement> — from: <run> <article>`
     under a section; retire a lesson by deleting its line. The judge reads all three sections,
     the investigator only "Search heuristics", the skeptic only "False-positive patterns".
     Nothing here changes budgets, tools or verdict rules. -->

## Search heuristics

## Fragile assumptions

## False-positive patterns
